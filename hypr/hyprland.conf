exec-once = wbg /home/aqusha/wallpaper/red.jpg
exec-once = waybar

monitor = DP-1, 1920x1080@100, 0x0, 1
monitor = HDMI-A-1, 1920x1080@100, 1920x0, 1

workspace = 1, monitor:DP-1
workspace = 2, monitor:DP-1
workspace = 3, monitor:HDMI-A-1
workspace = 4, monitor:HDMI-A-1

windowrule = match:class steam, workspace 1 silent
windowrule = match:class firefox, workspace 3 silent
windowrule = match:title Telegram, workspace 4 silent
windowrule = match:title obsidian, workspace 4 silent

bind = SUPER, C, exec, FILE="$HOME/Screenshots/$(date +'%Y-%m-%d-%H%M%S').png" && grim -g "$(slurp)" "$FILE" && wl-copy < "$FILE"
bind = SUPER SHIFT, C, exec, sh -c 'FILE="$HOME/Screenshots/$(date +%Y-%m-%d-%H%M%S).png"; grim -o "$(hyprctl activeworkspace -j | jq -r .monitor)" "$FILE" && wl-copy < "$FILE"'

monitor=,preferred,auto,auto

$terminal = kitty
$fileManager = nemo
$menu = rofi

env = XCURSOR_SIZE,20
env = HYPRCURSOR_SIZE,20
env = GTK_THEME,Adwaita-dark
env = GTK_APPLICATION_PREFER_DARK_THEME,1
env = ADW_DEBUG_COLOR_SCHEME,prefer-dark
env = GTK_THEME,Adwaita-dark

general {
    gaps_in = 8
    gaps_out = 14

    border_size = 2

    col.active_border = rgba(e32636ff)
    col.inactive_border = rgba(e32636cc)

    resize_on_border = false

    allow_tearing = false

    layout = dwindle
}

decoration {
    rounding = 15
    rounding_power = 3

    active_opacity = 0.9
    inactive_opacity = 0.9

    shadow {
        enabled = false
        range = 2
        render_power = 2
        color = rgba(e32636cc)
    }

    blur {
        enabled = true
        size = 2
        passes = 1

        vibrancy = 0.025
    }
}

animations {
    enabled = yes, please :)

    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, fade
    animation = workspacesIn,  1,     1.21,  almostLinear, fade
    animation = workspacesOut, 1,     1.94,  almostLinear, fade
    animation = zoomFactor,    1,     7,     quick
}

dwindle {
    preserve_split = true
}

misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
    disable_splash_rendering = true
}

input {
    kb_layout = us, ru
    kb_options = grp:win_space_toggle
    follow_mouse = 1

    sensitivity = 0
}

device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

$mainMod = SUPER

bind = $mainMod, W, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, D, togglefloating,
bind = $mainMod, F, fullscreen, 0
bind = $mainMod SHIFT, F, fullscreen, 1
bind = SUPER, R, exec, rofi -show drun

bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bindel = ,code:123, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bindel = ,code:122, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
bind = ,code:121, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
